version:2.1
orbs:
 node: circleci/node@4.7.0
jobs:
 build:
  executor:
   name: node/default
   tag: '10.4'
  steps:
  - checkout1
  - node/with-cache:
    steps:
     -run: npm install
  -run: npm rum test
